# Reproduction

1. Clone the repository.
2. Read `BUILD_STATUS.md`.
3. Run `tools/verify-release.ps1` on Windows or `python tools/verify_release.py`.
4. Use the synthetic fixtures only.
5. Open `evidence-site/index.html` for generated review evidence.

Native closure instructions will replace generated-review assumptions later.
